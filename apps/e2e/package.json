{
  "name": "@mirage/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:soak": "MIRAGE_RUN_SOAK=1 vitest run src/run-1m-soak.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1050.0",
    "@aws-sdk/lib-storage": "^3.1050.0",
    "bullmq": "^5.34.5",
    "ioredis": "^5.4.2",
    "mongodb": "^6.12.0",
    "pino": "^9.5.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "vitest": "^2.1.8"
  },
  "nx": {
    "name": "@mirage/e2e",
    "targets": {
      "test": {
        "executor": "nx:run-commands",
        "options": {
          "command": "vitest run",
          "cwd": "apps/e2e"
        }
      },
      "test:soak": {
        "executor": "nx:run-commands",
        "options": {
          "command": "MIRAGE_RUN_SOAK=1 vitest run src/run-1m-soak.test.ts",
          "cwd": "apps/e2e"
        }
      },
      "typecheck": {
        "executor": "nx:run-commands",
        "options": {
          "command": "tsc -p tsconfig.json --noEmit",
          "cwd": "apps/e2e"
        }
      },
      "lint": {
        "executor": "nx:run-commands",
        "options": {
          "command": "eslint src",
          "cwd": "apps/e2e"
        }
      }
    }
  }
}
