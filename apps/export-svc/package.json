{
  "name": "@mirage/export-svc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.717.0",
    "@mirage/auth": "workspace:*",
    "@mirage/connectors": "workspace:*",
    "@mirage/types": "workspace:*",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  },
  "nx": {
    "name": "@mirage/export-svc",
    "targets": {
      "serve": {
        "executor": "nx:run-commands",
        "options": {
          "command": "tsx watch --env-file=../../.env src/main.ts",
          "cwd": "apps/export-svc"
        }
      },
      "typecheck": {
        "executor": "nx:run-commands",
        "options": {
          "command": "tsc -p tsconfig.json --noEmit",
          "cwd": "apps/export-svc"
        }
      },
      "lint": {
        "executor": "nx:run-commands",
        "options": {
          "command": "eslint src",
          "cwd": "apps/export-svc"
        }
      }
    }
  }
}
